# Step 2 — Generated Prompt (Result of the Reusable Prompt)

```
Develop a comprehensive, professional-level learning module on **“The Feedback Loop and Velocity: How Early Performance Buys Distribution”** as part of an advanced course on the algorithmic allocation of attention — how platforms decide what each person sees, and what that decision rewards. The module should how the early performance buys the distribution — the test-and-expand, the velocity, the bandit logic, and the loop's dynamics — and how to design for the first window.

The material should teach the velocity layer: the two-stage test, the early metrics, the allocation logic, and the first window as the strategy's auction. Go beyond the 'post and hope' version of distribution, and the 'the algorithm is rigged' version as its opposite and examine the feedback loop as the engine's dynamic core — the amplification, the instability, and the correction — with the first window as the designed entry.

Teach the two-stage test: the content's seed distribution (the small audience — the follows, the interest pool, the exploration slice), the performance read (the early metrics against the seed's expected), and the expansion decision (the promotion to the larger pool, the halt) — the documented pattern across the short-video platforms, and the stages' dynamics.

Teach the velocity metrics: the early engagement per unit of the seed (the rate, not the count — the normalized signal), the completion in the first window, the re-watch, the share, and the early comment — the metrics the read uses, the window per platform, and the normalization that makes the signal comparable.

Teach the bandit logic: the exploration-exploitation problem (the allocation across the candidates — the known good versus the untested), the formal models (the UCB, the Thompson sampling — the confidence and the posterior as the allocation's logic), and the platform's informal version (the exploration slice, the test pool) — the logic, and the strategy's read of it (the new content's exploration budget, the decaying with the age).

Cover the loop's dynamics: the amplification (the strong content's expanding reach, the engagement that funds the next expansion), the starvation (the weak content's halt, the seed's exhaustion without the expansion), and the instability (the feedback's runaway — the viral spike, the correction — and the quality layer's check on the runaway) — the three dynamics, the evidence, and the platform's stabilizers.

Teach the first window as the auction: the content's early design — the hook (the first seconds' holding power), the completion design (the structure that earns the finish), the early engagement seed (the first comments' prompt, the reply's presence), and the posting context (the time, the series' position) — the entry designed against the read's metrics, and the window's honesty (the first window's signal, not the verdict — the correction, the late bloom, the quality layer).

Include practical methods for working at a professional level:
* The two-stage map per platform: the seed's composition, the read's metrics, the expansion's trigger, the documented status
* The velocity computation: the per-seed normalization, the window, the comparable index
* The bandit read: the exploration allocation's inference, the age decay, the content's exploration budget
* The loop diagnostics: the amplification's signature, the starvation's signature, the runaway's signature, in the reach data
* The first-window spec: the hook, the completion, the seed prompt, the context — with the design rule per element

Include practical exercises that require the learner to:
1. Map the two-stage test for two platforms: the seed's composition, the read's metrics, the expansion's trigger — with the documentation status per element.
2. Compute the velocity index for a content set: the per-seed normalization, the window, the comparable ranking — and the diagnosis of the bottom decile.
3. Infer the exploration allocation from a platform's behavior: the new content's seed pattern, the age decay, and the exploration budget's estimate.
4. Run the loop diagnostics on a reach dataset: the amplification, the starvation, the flatline signatures — with the case per dynamic.
5. Write the first-window spec for a content type: the hook, the completion, the seed prompt, the context — with the design rule and the metric per element.
6. Write the ethics note for the auction strategy: the seed prompt's line, the reaction's honesty, the runaway's share — as the standing policy.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively, beginning with the two-stage test and the velocity, moving through the bandit logic and the loop's dynamics, then to the first window, designed and read against the loop. Establish clear conceptual distinctions between **seed, read, expansion, velocity, and the loop's three dynamics** before showing how they interact.

Use professional terminology from information retrieval, recommender systems, and platform economics where relevant, but explain specialized terminology in clear language. Do not make the material sound like generic AI-generated advice; the register should be that of an advanced professional training module used by content strategists, platform analysts, and creators optimizing for algorithmic distribution.

Research the subject using high-quality professional and academic sources where external research materially strengthens the material. Prioritize authoritative sources: the recommender-systems literature (the collaborative-filtering and matrix-factorization papers, the bandit and contextual-bandit literature, the Netflix/Amazon case papers), the platforms' own transparency and documentation materials, and the empirical studies of feed optimization and its effects. Avoid relying primarily on low-quality SEO articles, generic recommendation engines blogs, or unsupported “best practice” claims. Where useful, distinguish established research findings from professional conventions and informed recommendations.

Pay particular attention to the relationship between this module and principles such as **the rate over the count, the window as claim, the auction as design, and the quality layer as check**. Connect these concepts to concrete analytical and design decisions rather than discussing them only theoretically.

Create a professional framework that content strategists, platform analysts, and creators optimizing for algorithmic distribution could actually use in a real project. The final material should therefore function simultaneously as a learning resource, a practical reference, and a working methodology for designing content and strategy for algorithmically distributed platforms.

Throughout the module, prioritize language that is:

* Precise without being jargon-heavy
* Empirical without being statistical theater
* Confident about what the evidence establishes
* Explicit about what the evidence does not establish
* Conditional where the findings are conditional
* Practical where the decisions are practical

Where a claim depends on context (platform, audience, topic, measurement), explicitly explain the trade-off and the boundary condition rather than presenting it as an absolute rule.

End the module with **Two-stage map checklist**, **Velocity checklist**, **First window checklist**, and a **Delivery gate checklist** that a professional team can use before acting on designing content and strategy for algorithmically distributed platforms.

The final result should be comprehensive enough to serve as an advanced professional training module, but organized clearly enough that a learner can study it progressively and apply each concept in designing content and strategy for algorithmically distributed platforms.
```
