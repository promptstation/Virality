# Step 1 — Reusable Prompt (Template)

Reusable prompt for the "section → learning-module prompt" task. The **task** is the section's Summary plus its Absorbed Skills, as produced by the research outline. Fill the placeholders, run it, and its output is the Step 2 prompt.

```
Generate a prompt on the task:

{SECTION_TITLE} — {SECTION_SUMMARY}

Absorbed skills:
- {ABSORBED_SKILL_1}
- {ABSORBED_SKILL_2}
- {ABSORBED_SKILL_3}
...

You are free to install software, programs and write codes, research the internet so as to meet our goals.
You are free to search the internet, books and make sure that you have selected those of professional levels. You are also free to install softwares, programs and write codes as long as it helps in achieving the goal.
At the end let me have the result.
```

---

# Filled Instance — Course 12 · Section 07

```
Generate a prompt on the task:

Simulation and Scenario Planning: Modeling the Unpredictable — The instrument's use: the simulation per the model, the scenario per the parameter. The section teaches the simulation's design — the model per the system (the agent per the data, the rule per the observation, the environment per the system), the parameter per the estimate (the rate per the interaction, the threshold per the agent, the density per the network), and the run per the ensemble (the run per the parameter, the distribution per the ensemble, the measure per the run) — with the record per the model. It covers the scenario's design — the baseline per the estimate (the state per the parameter, the trajectory per the time), the alternative per the parameter (the state per the parameter, the trajectory per the time), and the stress per the perturbation (the state per the perturbation, the recovery per the time) — with the measure per the scenario and the record per the scenario. It teaches the scenario's use — the expectation per the baseline, the preparation per the alternative, and the resilience per the stress (the state per the scenario, the design per the scenario) — with the frame per the scenario. It ends with the honest frame (the simulation as the exploration, not the prediction; the scenario as the preparation, not the forecast; the limit per the observation) and the operation (the model per the element, the parameter per the estimate, the run per the ensemble, the scenario per the design, the use per the expectation) with the record per the model.

Absorbed skills:
- Design and run the simulation — model per the system, parameter per the estimate, run per the ensemble — and design the scenario — baseline, alternative, stress — with the measure per the scenario
- Use the scenario per the expectation — the baseline, the preparation, the resilience — with the honest frame and the record per the model

You are free to install software, programs and write codes, research the internet so as to meet our goals.
You are free to search the internet, books and make sure that you have selected those of professional levels. You are also free to install softwares, programs and write codes as long as it helps in achieving the goal.
At the end let me have the result.
```
