---
name: simulation-scenario-planning
description: Build comprehensive, professional-level learning modules, evidence briefs, case diagnoses, and strategy documents on Simulation and Scenario Planning: Modeling the Unpredictable — how the simulation and the scenario are designed and run — model per the system, parameter per the estimate, run per the ensemble, scenario per the design, use per the expectation. Use this skill whenever the user asks to teach, explain, research, synthesize, critique, or design around simulation, scenario planning, ensemble simulation, parameter sensitivity, baseline scenario, stress test, monte carlo viral, what-if modeling, scenario design. Apply it even when the user never says “learning module” — if they request a brief, memo, workshop, training, or an analysis of the future is not predictable — how is it modeled, what scenarios are designed, and how is the preparation per the scenario, use this skill.
compatibility: Text-only environment; web research permitted for primary sources and canonical works. No special tools required.
metadata:
  author: Promptstation
  version: 1.0.0
  category: complex-systems-emergence
---

# Simulation and Scenario Planning: Modeling the Unpredictable: Professional Module Builder

## Mission

Produce professional-level material on how the simulation and the scenario are designed and run — model per the system, parameter per the estimate, run per the ensemble, scenario per the design, use per the expectation. The unit of work is a defensible artifact — a learning module, an evidence brief, a case diagnosis, or a strategy memo — in which every claim is tied to a source, a mechanism, and a boundary condition.

Optimize simultaneously for:

- empirical precision (what the canonical studies and works actually found)
- conceptual clarity (the core terms kept strictly distinct)
- causal discipline (correlational and causal claims never blurred)
- practical utility (a practitioner can act on the output the same day)
- intellectual honesty (limits and boundary conditions as visible as the findings)

Do not produce generic advice or pop-science hype. The register is that of an advanced professional training program: confident where the evidence is strong, explicitly conditional where it is not.

## Use Cases

### New professional learning module
When asked to create a full module, course unit, or workshop on this topic:
1. Establish scope: audience, depth, and which claims the module must cover.
2. Build the evidence base from primary sources and canonical works.
3. Develop the core mechanisms and diagnostic logic.
4. Add method, critique, and application sections.
5. End with exercises and the delivery checklists.
6. Deliver the complete artifact with no placeholders.

### Brief or leadership memo
When asked for a short document that must decide something:
1. State the question the document must answer in one sentence.
2. Include only findings strong enough to bear that decision, each with its mechanism and source.
3. Convert results into plain-language implications.
4. End with the specific decisions the evidence changes.
5. Cap length; cut context before cutting caveats.

### Case diagnosis
When asked to explain why a specific piece of content, campaign, or product spread (or did not):
1. Identify the operative mechanisms — never a single assumed cause.
2. Map the case onto the diagnostic framework of this skill.
3. Run the differential: which mechanisms were active, which alternatives were considered and set aside.
4. State confidence and the evidence that would revise the diagnosis.

### Strategy design
When asked to design a campaign, content system, or program around this lever:
1. Fix the objective, audience, and platform context.
2. Select the mechanisms from this skill that fit that context.
3. Specify implementation, sequencing, and the measurement plan.
4. State the boundary conditions under which the plan breaks.

## Core Output Requirements

Regardless of format:

- Every named finding or claim carries its source or mechanism in the same breath.
- Correlational and causal claims are labeled as such wherever they matter.
- Effect claims carry their metric and a plain-language rendering.
- Boundary conditions are stated as explicit “applies when / breaks down when” conditions.
- Examples use real, named studies, campaigns, or artifacts — not invented case studies.
- No padding sections, generic intro/outro filler, or advice that would read the same for any topic.

## Phase 1 — Scope and Claim Audit

### Artifact and audience
What the user actually needs (module vs. memo vs. diagnosis) and who reads it. Different readers make different cuts of the same material.

### Claim inventory
List the claims the artifact will make. For each, note the source or mechanism that supports it and whether that support is correlational, experimental, or canonical. Downgrade or cut unsupported claims — not the caveat.

### Depth budget
Match length to the decision the artifact serves. The fix for a weak document is choosing stronger claims to carry it, not adding hedges to weaker ones.

## Phase 2 — Foundations and Core Content

1. Teach the simulation's design: the model per the system (the agent per the data, the rule per the observation, the environment per the system), the parameter per the estimate (the rate per the interaction, the threshold per the agent, the density per the network), and the run per the ensemble (the run per the parameter, the distribution per the ensemble, the measure per the run) — with the record per the model.
2. Teach the scenario's design: the baseline per the estimate (the state per the parameter, the trajectory per the time), the alternative per the parameter (the state per the parameter, the trajectory per the time), and the stress per the perturbation (the state per the perturbation, the recovery per the time) — with the measure per the scenario and the record per the scenario.
3. Teach the scenario's use: the expectation per the baseline (the state per the trajectory, the expectation per the baseline), the preparation per the alternative (the state per the alternative, the preparation per the alternative), and the resilience per the stress (the state per the stress, the resilience per the stress) — with the design per the scenario and the record per the scenario.
4. Teach the honest frame: the simulation as the exploration, not the prediction (the run per the ensemble, the distribution per the ensemble), the scenario as the preparation, not the forecast (the design per the scenario, the expectation per the baseline), and the limit per the observation (the state per the data, the boundary per the element) — with the frame per the element.
5. Teach the operation: the model per the element (the agent, the rule, the environment), the parameter per the estimate (the rate, the threshold, the density), the run per the ensemble (the run, the distribution, the measure), the scenario per the design (the baseline, the alternative, the stress), and the use per the expectation (the expectation, the preparation, the resilience) — with the record per the model and the review per the cycle.

## Phase 3 — Method and Technique

- The simulation spec: the model per the system, the parameter per the estimate, the run per the ensemble
- The scenario spec: the baseline per the estimate, the alternative per the parameter, the stress per the perturbation
- The use spec: the expectation per the baseline, the preparation per the alternative, the resilience per the stress
- The frame spec: the simulation per the exploration, the scenario per the preparation, the limit per the observation
- The operation spec: the model per the element, the parameter per the estimate, the run per the ensemble, the scenario per the design, the use per the expectation

## Phase 4 — Analysis and Diagnosis

- Diagnose the simulation's state per element: the model's gap (the agent per the data, the rule per the observation, the environment per the system), the parameter's gap (the rate per the interaction, the threshold per the agent, the density per the network), the run's gap (the run per the parameter, the distribution per the ensemble) — the element per the gap, and the fix per element.
- Read the scenario's state per the design: the baseline per the estimate, the alternative per the parameter, the stress per the perturbation — the state per the scenario.
- Check the use per the expectation: the expectation per the baseline, the preparation per the alternative, the resilience per the stress — the design per the scenario.
- Audit the frame per the element: the simulation per the exploration, the scenario per the preparation, the limit per the observation — the breach per the case.

## Phase 5 — Application and Design

- Run the simulation per the design: the model per the system, the parameter per the estimate, the run per the ensemble — with the record per the model.
- Run the scenario per the design: the baseline per the estimate, the alternative per the parameter, the stress per the perturbation — with the record per the scenario.
- Run the use per the expectation: the expectation per the baseline, the preparation per the alternative, the resilience per the stress — with the record per the scenario.
- Run the frame per the element: the simulation per the exploration, the scenario per the preparation, the limit per the observation — with the record per the model.

## Phase 6 — Ethics and Boundary Conditions

- The simulation's run (the ensemble per the parameter) is the model's exploration per the data; its use (the expectation per the baseline, the preparation per the alternative) is per the honesty of the model.
- The scenario's design (the baseline, the alternative, the stress) is the analyst's construction per the parameter; its use (the preparation per the scenario, the resilience per the stress) is per the honesty of the construction.
- The limit (the observation per the data) is the instrument's boundary per the element; its use (the expectation per the boundary, the decision per the limit) is per the honesty of the boundary.

## Phase 7 — Exercises and Assessment

For learning artifacts, include exercises that force the diagnosis skill, not recall. Use the standard set:

1. Spec the simulation for a viral system: the model per the system, the parameter per the estimate, the run per the ensemble — with the record per the model.
2. Design the scenario: the baseline per the estimate, the alternative per the parameter, the stress per the perturbation — with the measure per the scenario.
3. Run the use per the expectation: the expectation per the baseline, the preparation per the alternative, the resilience per the stress — with the record per the scenario.
4. Run the frame on a case: the simulation per the exploration, the scenario per the preparation, the limit per the observation — with the record per the model.
5. Run the operation per the cycle: the model per the element, the parameter per the estimate, the run per the ensemble, the scenario per the design, the use per the expectation — with the review per the cycle.
6. Write the simulation's operating document: the simulation, scenario, use, frame, and operation specs — as the standing instrument, with the owner.

For each exercise provide scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

## Phase 8 — Quality Gate and Delivery

Before delivering, run the delivery checklists. The artifact fails the gate on any “no.”

### Simulation
- The model is per the system
- The parameter is per the estimate
- The run is per the ensemble

### Scenario
- The baseline is per the estimate
- The alternative is per the parameter
- The stress is per the perturbation

### Use
- The expectation is per the baseline
- The preparation is per the alternative
- The resilience is per the stress

### Delivery gate
- The frame is per the element
- The operation is per the cycle
- The record is per the model

## Anti-Patterns

the simulation as the prediction; the scenario as the forecast; the parameter without the estimate; the run without the ensemble; the baseline without the alternative; the stress unexamined.

## Decision Heuristic

Before any claim or recommendation, ask:

1. What is the model per the system, and what is the parameter per the estimate?
2. What is the run per the ensemble, and what is the distribution per the ensemble?
3. What is the scenario per the design — baseline, alternative, stress?
4. What is the use per the expectation, and what is the design per the scenario?
5. What is the frame per the element, and what is the record per the model?

If a claim cannot survive all of these questions, it is not in the artifact.

## Final Principle

The goal is not the future's prediction. It is the simulation per the design — model per the system, parameter per the estimate, run per the ensemble, scenario per the design, use per the expectation — so the system's future is explored on its logic, and the logic says where it is sure.
