# Step 2 — Generated Prompt (Result of the Reusable Prompt)

```
Develop a comprehensive, professional-level learning module on **“What Makes a System Complex: Components, Interactions, and Scale”** as part of an advanced course on the science of systems with many interacting parts — and why the behavior of such systems (tipping, nonlinearity, emergence) is the shape of viral spread. The module should what makes a system complex — component, interaction, scale — and how the boundary and state are set per the question and the measure.

The material should teach the definition: the element per the complexity, the boundary per the question, the state per the measure, the limit per the observation. Go beyond complexity as size, and the system as a whole without its elements and examine complexity as logic with the element per the definition — component, interaction, scale — and the boundary as the instrument's input.

Teach the component: the agents per the system — the user (knowledge, preference, connection), the content (form, meaning, age), the platform (rules, amplification, medium), the institution (policy, norm, constraint) — the state per component, with the measure per element.

Teach the interaction: the coupling per component — transmission (content per user: share, view, adoption), imitation (user per user: copy, remix, response), response (system per component: feedback, adaptation, selection) — the rule per interaction, with the measure per element.

Teach the scale: the composition per interaction — the local rule (the pair's interaction), the global state (the population's or platform's composition), and emergence (the global per the local: the state per the rule, the behavior per the composition) — with the measure per scale and the limit per observation.

Teach the distinction from largeness: the large system with the simple interaction (state per element, composition per sum) versus the small system with the complex interaction (state per rule, behavior per composition) — complexity per the interaction and the scale, not the size.

Teach the boundary, state, and observation's limit: the boundary per the question (network for the spread, platform for the conversation, market for the adoption), the state per the definition (per component, per system), and the limit (visible per the platform — reach, engagement; hidden per the model — inference) — the frame per element.

Include practical methods for working at a professional level:
* The component inventory: the agent per the system, the state per the component, the measure per the element
* The interaction map: the coupling per the component, the rule per the interaction, the measure per the element
* The scale analysis: the local rule, the global state, the emergence per the composition
* The boundary spec: the system per the question, the element per the boundary, the record per the case
* The state spec: the state per the component and system, the observation's limit per the data

Include practical exercises that require the learner to:
1. Build the component inventory for a viral system: the agent per the element, the state per the component, the measure per the element.
2. Build the interaction map: the coupling per the component, the rule per the interaction, the measure per the element.
3. Run the scale analysis: the local rule, the global state, the emergence per the composition — with the measure per scale and the limit per observation.
4. Set the boundary per the question: the system per the question, the element per the boundary — with the record per case.
5. Spec the state per the measure: the state per the component and system, the observation's limit per the data.
6. Write the foundations' operating document: the inventory, the map, the scale, the boundary, the state — as the standing definition, with the owner.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively, beginning with the component and the interaction, moving through the scale and the distinction from largeness, then to the boundary and the state, running as the definition. Establish clear conceptual distinctions between **component, interaction, scale, boundary, and state** before showing how they interact.

Use professional terminology from complex systems science, network science, and dynamical systems where relevant, but explain specialized terminology in clear language. Do not make the material sound like generic AI-generated advice; the register should be that of an advanced professional training module used by strategists, researchers, and modelers who need the system's logic, not just the metric.

Research the subject using high-quality professional and academic sources where external research materially strengthens the material. Prioritize authoritative sources: the complex-systems core literature (Watts' complex systems work, Barabási's network science, the percolation and phase-transition literature), the emergence and self-organized-criticality literature, and the agent-based-modeling practice literature. Avoid relying primarily on low-quality SEO articles, generic complex systems emergence blogs, or unsupported “best practice” claims. Where useful, distinguish established research findings from professional conventions and informed recommendations.

Pay particular attention to the relationship between this module and principles such as **the complexity per the interaction and the scale, the boundary per the question, the state per the measure, and the limit per the observation**. Connect these concepts to concrete analytical and design decisions rather than discussing them only theoretically.

Create a professional framework that strategists, researchers, and modelers who need the system's logic, not just the metric could actually use in a real project. The final material should therefore function simultaneously as a learning resource, a practical reference, and a working methodology for reasoning about viral phenomena as system behavior.

Throughout the module, prioritize language that is:

* Precise without being jargon-heavy
* Empirical without being statistical theater
* Confident about what the evidence establishes
* Explicit about what the evidence does not establish
* Conditional where the findings are conditional
* Practical where the decisions are practical

Where a claim depends on context (platform, audience, topic, measurement), explicitly explain the trade-off and the boundary condition rather than presenting it as an absolute rule.

End the module with **Component checklist**, **Interaction checklist**, **Scale checklist**, and a **Delivery gate checklist** that a professional team can use before acting on reasoning about viral phenomena as system behavior.

The final result should be comprehensive enough to serve as an advanced professional training module, but organized clearly enough that a learner can study it progressively and apply each concept in reasoning about viral phenomena as system behavior.
```
