---
name: agent-based-modeling
description: Build comprehensive, professional-level learning modules, evidence briefs, case diagnoses, and strategy documents on Agent-Based Thinking: Models of Individuals, Rules, and Local Interaction — how the agent-based model is built and run — agent, rule, environment, simulation, honesty — and how it is used per the question. Use this skill whenever the user asks to teach, explain, research, synthesize, critique, or design around agent-based model, ABM, agent simulation, local interaction model, micro to macro, stochastic simulation, parameter calibration, model validation. Apply it even when the user never says “learning module” — if they request a brief, memo, workshop, training, or an analysis of the system's behavior is not predictable from the parts — how is it modeled, run, and read, with the honesty per the limit, use this skill.
compatibility: Text-only environment; web research permitted for primary sources and canonical works. No special tools required.
metadata:
  author: Promptstation
  version: 1.0.0
  category: complex-systems-emergence
---

# Agent-Based Thinking: Models of Individuals, Rules, and Local Interaction: Professional Module Builder

## Mission

Produce professional-level material on how the agent-based model is built and run — agent, rule, environment, simulation, honesty — and how it is used per the question. The unit of work is a defensible artifact — a learning module, an evidence brief, a case diagnosis, or a strategy memo — in which every claim is tied to a source, a mechanism, and a boundary condition.

Optimize simultaneously for:

- empirical precision (what the canonical studies and works actually found)
- conceptual clarity (the core terms kept strictly distinct)
- causal discipline (correlational and causal claims never blurred)
- practical utility (a practitioner can act on the output the same day)
- intellectual honesty (limits and boundary conditions as visible as the findings)

Do not produce generic advice or pop-science hype. The register is that of an advanced professional training program: confident where the evidence is strong, explicitly conditional where it is not.

## Use Cases

### New professional learning module
When asked to create a full module, course unit, or workshop on this topic:
1. Establish scope: audience, depth, and which claims the module must cover.
2. Build the evidence base from primary sources and canonical works.
3. Develop the core mechanisms and diagnostic logic.
4. Add method, critique, and application sections.
5. End with exercises and the delivery checklists.
6. Deliver the complete artifact with no placeholders.

### Brief or leadership memo
When asked for a short document that must decide something:
1. State the question the document must answer in one sentence.
2. Include only findings strong enough to bear that decision, each with its mechanism and source.
3. Convert results into plain-language implications.
4. End with the specific decisions the evidence changes.
5. Cap length; cut context before cutting caveats.

### Case diagnosis
When asked to explain why a specific piece of content, campaign, or product spread (or did not):
1. Identify the operative mechanisms — never a single assumed cause.
2. Map the case onto the diagnostic framework of this skill.
3. Run the differential: which mechanisms were active, which alternatives were considered and set aside.
4. State confidence and the evidence that would revise the diagnosis.

### Strategy design
When asked to design a campaign, content system, or program around this lever:
1. Fix the objective, audience, and platform context.
2. Select the mechanisms from this skill that fit that context.
3. Specify implementation, sequencing, and the measurement plan.
4. State the boundary conditions under which the plan breaks.

## Core Output Requirements

Regardless of format:

- Every named finding or claim carries its source or mechanism in the same breath.
- Correlational and causal claims are labeled as such wherever they matter.
- Effect claims carry their metric and a plain-language rendering.
- Boundary conditions are stated as explicit “applies when / breaks down when” conditions.
- Examples use real, named studies, campaigns, or artifacts — not invented case studies.
- No padding sections, generic intro/outro filler, or advice that would read the same for any topic.

## Phase 1 — Scope and Claim Audit

### Artifact and audience
What the user actually needs (module vs. memo vs. diagnosis) and who reads it. Different readers make different cuts of the same material.

### Claim inventory
List the claims the artifact will make. For each, note the source or mechanism that supports it and whether that support is correlational, experimental, or canonical. Downgrade or cut unsupported claims — not the caveat.

### Depth budget
Match length to the decision the artifact serves. The fix for a weak document is choosing stronger claims to carry it, not adding hedges to weaker ones.

## Phase 2 — Foundations and Core Content

1. Teach the agent: the state per agent — the knowledge (exposure, adoption), the preference (fit, affinity), the connection (link, weight), and the action (transmission, imitation, response) — with the distribution per measure and the record per the model.
2. Teach the rule: the behavior per state — transmission per contact (the probability per the pair), imitation per neighbor (the adoption per the exposure), response per feedback (the adaptation per the feedback) — with the probability per rule and the measure per element.
3. Teach the environment: the space per agent — the network (link, weight, community), the platform (channel, amplification, format), the market (competition, demand, price) — with the structure per environment and the record per the model.
4. Teach the construction: the agent per the data (the distribution per measure), the rule per the observation (the rate per the data), the environment per the system, the initialization per the state (the seed per the model), and the parameter per the estimate (rate, probability, weight per element).
5. Teach the simulation, honesty, and use: the run (the step per the model, the observation per the step, the ensemble per the parameter), the honesty (the simplification per element, the validation per the data, the limit per the observation), and the use (the spread per the behavior, the trend per the imitation, the tipping per the composition) — with the frame and the record per the model.

## Phase 3 — Method and Technique

- The agent spec: the state per the agent, the distribution per the measure
- The rule spec: the probability per the rule, the rate per the data
- The environment spec: the structure per the environment, the medium per the platform
- The simulation spec: the step per the model, the observation per the step, the ensemble per the parameter
- The honesty spec: the simplification per the element, the validation per the data, the limit per the observation

## Phase 4 — Analysis and Diagnosis

- Diagnose the model's failure per element: the agent's gap (the state per the data, the distribution per the measure), the rule's gap (the probability per the observation, the rate per the data), the environment's gap (the structure per the system, the medium per the platform) — the element per the gap, and the fix per element.
- Check the simulation's state: the step per the model, the observation per the step, the measurement per the run, the ensemble per the parameter — the gap per the run.
- Read the honesty's state: the simplification per the element, the validation per the data, the limit per the observation — the breach per the model.
- Audit the use per the question: the question per the model, the answer per the simulation — the gap per the question.

## Phase 5 — Application and Design

- Run the construction per element: the agent per the data, the rule per the observation, the environment per the system, the initialization per the state, the parameter per the measure — with the record per the model.
- Run the simulation per the run: the step per the model, the observation per the step, the ensemble per the parameter — with the record per the run.
- Run the honesty per the limit: the simplification per the element, the validation per the data, the limit per the observation — with the record per the model.
- Run the use per the question: the spread per the behavior, the trend per the imitation, the tipping per the composition — with the answer per the simulation and the record per the question.

## Phase 6 — Ethics and Boundary Conditions

- The agent's state (knowledge, preference, connection) is the person's data per the model; the construction per element is per the consent and the minimization.
- The model's simplification (the element per the model) is the analyst's construction; its use (the claim per the model, the strategy per the simulation) is per the honesty of the simplification.
- The validation (the model per the data) is the instrument's integrity; its use (the trust per the model, the decision per the simulation) is per the validation's state.

## Phase 7 — Exercises and Assessment

For learning artifacts, include exercises that force the diagnosis skill, not recall. Use the standard set:

1. Spec the agent for a viral system: the state per the agent, the distribution per the measure — per element, with the record per the model.
2. Spec the rule per the observation: the probability per the rule, the rate per the data — per element, with the record per the model.
3. Spec the environment per the system: the structure per the environment, the medium per the platform.
4. Run the simulation per the run: the step per the model, the observation per the step, the ensemble per the parameter.
5. Run the honesty per the model: the simplification per the element, the validation per the data, the limit per the observation.
6. Write the model's operating document: the agent, rule, environment, simulation, and honesty specs — as the standing instrument, with the owner.

For each exercise provide scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

## Phase 8 — Quality Gate and Delivery

Before delivering, run the delivery checklists. The artifact fails the gate on any “no.”

### Construction
- The agent is per the data
- The rule is per the observation
- The environment is per the system

### Simulation
- The step is per the model
- The observation is per the step
- The ensemble is per the parameter

### Honesty
- The simplification is per the element
- The validation is per the data
- The limit is per the observation

### Delivery gate
- The initialization is per the state
- The parameter is per the measure
- The use is per the question, with the record

## Anti-Patterns

the model as the prediction; the simulation as the truth; the agent without the state; the rule without the probability; the environment without the structure; the parameter without the estimate; the validation skipped.

## Decision Heuristic

Before any claim or recommendation, ask:

1. What is the agent's state per the data, and what is the distribution?
2. What is the rule per the observation, and what is the probability?
3. What is the environment per the system, and what is the structure?
4. What is the simulation per the run, and what is the ensemble per the parameter?
5. What is the honesty per the limit, and what is the use per the question?

If a claim cannot survive all of these questions, it is not in the artifact.

## Final Principle

The goal is not the prediction. It is the model per element — agent per data, rule per observation, environment per system, simulation per run, honesty per limit — so the system is explored on its logic, and the logic says where it is sure.
