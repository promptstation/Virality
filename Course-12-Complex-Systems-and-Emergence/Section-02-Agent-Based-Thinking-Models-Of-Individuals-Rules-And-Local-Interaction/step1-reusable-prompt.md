# Step 1 — Reusable Prompt (Template)

Reusable prompt for the "section → learning-module prompt" task. The **task** is the section's Summary plus its Absorbed Skills, as produced by the research outline. Fill the placeholders, run it, and its output is the Step 2 prompt.

```
Generate a prompt on the task:

{SECTION_TITLE} — {SECTION_SUMMARY}

Absorbed skills:
- {ABSORBED_SKILL_1}
- {ABSORBED_SKILL_2}
- {ABSORBED_SKILL_3}
...

You are free to install software, programs and write codes, research the internet so as to meet our goals.
You are free to search the internet, books and make sure that you have selected those of professional levels. You are also free to install softwares, programs and write codes as long as it helps in achieving the goal.
At the end let me have the result.
```

---

# Filled Instance — Course 12 · Section 02

```
Generate a prompt on the task:

Agent-Based Thinking: Models of Individuals, Rules, and Local Interaction — The method: the agent-based model as the system's instrument. The section teaches the model's structure — the agent (state per agent: knowledge, preference, connection, action), the rule (behavior per state: transmission per contact, imitation per neighbor, response per feedback), and the environment (space per agent: the network, the platform, the market) — with the simulation per the model. It covers the model's construction (the agent's definition per the data, the rule's specification per the observation, the environment's structure per the system, the initialization per the state, the parameter per the estimate), the simulation's run (the step per the model, the observation per the step, the measurement per the run, the ensemble per the parameter), and the model's honesty (the simplification per the element, the validation per the data, the limit per the observation). It ends with the use per the question — the spread per the agent's behavior, the trend per the imitation, the tipping per the composition — with the frame (the model as the instrument, the simulation as the exploration, the parameter as the estimate) and the record per the model.

Absorbed skills:
- Build the agent-based model — agent, rule, environment, initialization, parameter — with the simulation per the run and the ensemble per the parameter
- Hold the model's honesty — simplification, validation, limit — and use the model per the question, not as the prediction

You are free to install software, programs and write codes, research the internet so as to meet our goals.
You are free to search the internet, books and make sure that you have selected those of professional levels. You are also free to install softwares, programs and write codes as long as it helps in achieving the goal.
At the end let me have the result.
```
