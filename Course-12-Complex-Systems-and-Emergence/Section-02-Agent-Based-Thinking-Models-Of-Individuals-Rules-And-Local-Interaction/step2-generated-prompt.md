# Step 2 — Generated Prompt (Result of the Reusable Prompt)

```
Develop a comprehensive, professional-level learning module on **“Agent-Based Thinking: Models of Individuals, Rules, and Local Interaction”** as part of an advanced course on the science of systems with many interacting parts — and why the behavior of such systems (tipping, nonlinearity, emergence) is the shape of viral spread. The module should how the agent-based model is built and run — agent, rule, environment, simulation, honesty — and how it is used per the question.

The material should teach the method: the model per element, the simulation per run, the honesty per limit, the use per question. Go beyond the model as the prediction, and the simulation as the truth and examine the model as the instrument with the element per the definition — agent, rule, environment — and the honesty as the instrument's standing.

Teach the agent: the state per agent — the knowledge (exposure, adoption), the preference (fit, affinity), the connection (link, weight), and the action (transmission, imitation, response) — with the distribution per measure and the record per the model.

Teach the rule: the behavior per state — transmission per contact (the probability per the pair), imitation per neighbor (the adoption per the exposure), response per feedback (the adaptation per the feedback) — with the probability per rule and the measure per element.

Teach the environment: the space per agent — the network (link, weight, community), the platform (channel, amplification, format), the market (competition, demand, price) — with the structure per environment and the record per the model.

Teach the construction: the agent per the data (the distribution per measure), the rule per the observation (the rate per the data), the environment per the system, the initialization per the state (the seed per the model), and the parameter per the estimate (rate, probability, weight per element).

Teach the simulation, honesty, and use: the run (the step per the model, the observation per the step, the ensemble per the parameter), the honesty (the simplification per element, the validation per the data, the limit per the observation), and the use (the spread per the behavior, the trend per the imitation, the tipping per the composition) — with the frame and the record per the model.

Include practical methods for working at a professional level:
* The agent spec: the state per the agent, the distribution per the measure
* The rule spec: the probability per the rule, the rate per the data
* The environment spec: the structure per the environment, the medium per the platform
* The simulation spec: the step per the model, the observation per the step, the ensemble per the parameter
* The honesty spec: the simplification per the element, the validation per the data, the limit per the observation

Include practical exercises that require the learner to:
1. Spec the agent for a viral system: the state per the agent, the distribution per the measure — per element, with the record per the model.
2. Spec the rule per the observation: the probability per the rule, the rate per the data — per element, with the record per the model.
3. Spec the environment per the system: the structure per the environment, the medium per the platform.
4. Run the simulation per the run: the step per the model, the observation per the step, the ensemble per the parameter.
5. Run the honesty per the model: the simplification per the element, the validation per the data, the limit per the observation.
6. Write the model's operating document: the agent, rule, environment, simulation, and honesty specs — as the standing instrument, with the owner.

For each exercise, provide the scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

Structure the material progressively, beginning with the agent, the rule, the environment, moving through the construction and the simulation, then to the honesty and the use, running per the model. Establish clear conceptual distinctions between **agent, rule, environment, simulation, and honesty** before showing how they interact.

Use professional terminology from complex systems science, network science, and dynamical systems where relevant, but explain specialized terminology in clear language. Do not make the material sound like generic AI-generated advice; the register should be that of an advanced professional training module used by strategists, researchers, and modelers who need the system's logic, not just the metric.

Research the subject using high-quality professional and academic sources where external research materially strengthens the material. Prioritize authoritative sources: the complex-systems core literature (Watts' complex systems work, Barabási's network science, the percolation and phase-transition literature), the emergence and self-organized-criticality literature, and the agent-based-modeling practice literature. Avoid relying primarily on low-quality SEO articles, generic complex systems emergence blogs, or unsupported “best practice” claims. Where useful, distinguish established research findings from professional conventions and informed recommendations.

Pay particular attention to the relationship between this module and principles such as **the state per the data, the rule per the observation, the simulation per the run, the honesty per the limit, and the use per the question**. Connect these concepts to concrete analytical and design decisions rather than discussing them only theoretically.

Create a professional framework that strategists, researchers, and modelers who need the system's logic, not just the metric could actually use in a real project. The final material should therefore function simultaneously as a learning resource, a practical reference, and a working methodology for reasoning about viral phenomena as system behavior.

Throughout the module, prioritize language that is:

* Precise without being jargon-heavy
* Empirical without being statistical theater
* Confident about what the evidence establishes
* Explicit about what the evidence does not establish
* Conditional where the findings are conditional
* Practical where the decisions are practical

Where a claim depends on context (platform, audience, topic, measurement), explicitly explain the trade-off and the boundary condition rather than presenting it as an absolute rule.

End the module with **Construction checklist**, **Simulation checklist**, **Honesty checklist**, and a **Delivery gate checklist** that a professional team can use before acting on reasoning about viral phenomena as system behavior.

The final result should be comprehensive enough to serve as an advanced professional training module, but organized clearly enough that a learner can study it progressively and apply each concept in reasoning about viral phenomena as system behavior.
```
