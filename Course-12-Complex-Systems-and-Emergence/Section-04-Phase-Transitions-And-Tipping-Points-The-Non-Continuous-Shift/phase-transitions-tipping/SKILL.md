---
name: phase-transitions-tipping
description: Build comprehensive, professional-level learning modules, evidence briefs, case diagnoses, and strategy documents on Phase Transitions and Tipping Points: The Non-Continuous Shift — how tipping is modeled and read — parameter, order parameter, transition, early warning, hysteresis — with the honesty per the estimate and the limit. Use this skill whenever the user asks to teach, explain, research, synthesize, critique, or design around phase transition, tipping point, critical threshold, order parameter, hysteresis, early warning signals, critical slowing down, binary adoption, threshold model. Apply it even when the user never says “learning module” — if they request a brief, memo, workshop, training, or an analysis of is the system about to tip — what is the parameter, the state, the warning, and what is sure, use this skill.
compatibility: Text-only environment; web research permitted for primary sources and canonical works. No special tools required.
metadata:
  author: Promptstation
  version: 1.0.0
  category: complex-systems-emergence
---

# Phase Transitions and Tipping Points: The Non-Continuous Shift: Professional Module Builder

## Mission

Produce professional-level material on how tipping is modeled and read — parameter, order parameter, transition, early warning, hysteresis — with the honesty per the estimate and the limit. The unit of work is a defensible artifact — a learning module, an evidence brief, a case diagnosis, or a strategy memo — in which every claim is tied to a source, a mechanism, and a boundary condition.

Optimize simultaneously for:

- empirical precision (what the canonical studies and works actually found)
- conceptual clarity (the core terms kept strictly distinct)
- causal discipline (correlational and causal claims never blurred)
- practical utility (a practitioner can act on the output the same day)
- intellectual honesty (limits and boundary conditions as visible as the findings)

Do not produce generic advice or pop-science hype. The register is that of an advanced professional training program: confident where the evidence is strong, explicitly conditional where it is not.

## Use Cases

### New professional learning module
When asked to create a full module, course unit, or workshop on this topic:
1. Establish scope: audience, depth, and which claims the module must cover.
2. Build the evidence base from primary sources and canonical works.
3. Develop the core mechanisms and diagnostic logic.
4. Add method, critique, and application sections.
5. End with exercises and the delivery checklists.
6. Deliver the complete artifact with no placeholders.

### Brief or leadership memo
When asked for a short document that must decide something:
1. State the question the document must answer in one sentence.
2. Include only findings strong enough to bear that decision, each with its mechanism and source.
3. Convert results into plain-language implications.
4. End with the specific decisions the evidence changes.
5. Cap length; cut context before cutting caveats.

### Case diagnosis
When asked to explain why a specific piece of content, campaign, or product spread (or did not):
1. Identify the operative mechanisms — never a single assumed cause.
2. Map the case onto the diagnostic framework of this skill.
3. Run the differential: which mechanisms were active, which alternatives were considered and set aside.
4. State confidence and the evidence that would revise the diagnosis.

### Strategy design
When asked to design a campaign, content system, or program around this lever:
1. Fix the objective, audience, and platform context.
2. Select the mechanisms from this skill that fit that context.
3. Specify implementation, sequencing, and the measurement plan.
4. State the boundary conditions under which the plan breaks.

## Core Output Requirements

Regardless of format:

- Every named finding or claim carries its source or mechanism in the same breath.
- Correlational and causal claims are labeled as such wherever they matter.
- Effect claims carry their metric and a plain-language rendering.
- Boundary conditions are stated as explicit “applies when / breaks down when” conditions.
- Examples use real, named studies, campaigns, or artifacts — not invented case studies.
- No padding sections, generic intro/outro filler, or advice that would read the same for any topic.

## Phase 1 — Scope and Claim Audit

### Artifact and audience
What the user actually needs (module vs. memo vs. diagnosis) and who reads it. Different readers make different cuts of the same material.

### Claim inventory
List the claims the artifact will make. For each, note the source or mechanism that supports it and whether that support is correlational, experimental, or canonical. Downgrade or cut unsupported claims — not the caveat.

### Depth budget
Match length to the decision the artifact serves. The fix for a weak document is choosing stronger claims to carry it, not adding hedges to weaker ones.

## Phase 2 — Foundations and Core Content

1. Teach the structure: the parameter (the rate per the interaction, the threshold per the agent, the density per the network), the order parameter (the fraction per the population, the alignment per the platform), and the transition (the continuous per the gradual, the discontinuous per the jump) — with the measure per element and the example per phenomenon.
2. Teach the tipping point's model: the critical parameter (the threshold per the transition, the adoption per the threshold), the critical density (the percolation per the density, the cascade per the connectivity), and the critical rate (the spread per the rate, the saturation per the rate) — with the measure per element and the record per the case.
3. Teach the early warning: the slowing per the approach (the recovery per the time), the correlation per the proximity (the alignment per the pair), and the variance per the transition (the fluctuation per the state) — with the measure per element and the limit per observation.
4. Teach the hysteresis: the path per the parameter (the transition per the direction), the memory per the transition (the adoption per the path), and the irreversibility per the shift (the shift per the direction) — with the measure per element and the record per the case.
5. Teach the viral read and operation: the adoption per the threshold (the tipping per the threshold), the cascade per the density (the tipping per the density), and the trend per the rate (the tipping per the rate) — with the operation (parameter per measure, state per order, transition per shift, warning per element, hysteresis per history) and the record per the case.

## Phase 3 — Method and Technique

- The parameter spec: the rate per the interaction, the threshold per the agent, the density per the network
- The order spec: the fraction per the population, the alignment per the platform
- The transition spec: the continuous per the gradual, the discontinuous per the jump
- The warning spec: the slowing per the approach, the correlation per the proximity, the variance per the transition
- The hysteresis spec: the path per the parameter, the memory per the transition, the irreversibility per the shift

## Phase 4 — Analysis and Diagnosis

- Diagnose the transition's state per element: the parameter's gap (the rate, the threshold, the density), the order's gap (the fraction per the population, the alignment per the platform), the transition's gap (the continuous per the gradual, the discontinuous per the jump) — the element per the gap, and the fix per element.
- Read the warning per element: the slowing per the approach, the correlation per the proximity, the variance per the transition — the state per the warning.
- Check the hysteresis per the history: the path per the parameter, the memory per the transition, the irreversibility per the shift — the state per the history.
- Audit the tipping's honesty: the model per the estimate, the warning per the state, the limit per the observation — the breach per the case.

## Phase 5 — Application and Design

- Run the parameter per the measure: the rate per the interaction, the threshold per the agent, the density per the network — with the record per the case.
- Run the order per the state: the fraction per the population, the alignment per the platform — with the record per the case.
- Run the transition per the shift: the continuous per the gradual, the discontinuous per the jump — with the record per the case.
- Run the warning and hysteresis per element: the slowing, the correlation, the variance; the path, the memory, the irreversibility — with the record per the case.

## Phase 6 — Ethics and Boundary Conditions

- The parameter (rate, threshold, density) is the system's control per the measure; its use (the intervention per the transition, the design per the tipping) is per the estimate's honesty.
- The order parameter (the fraction, the alignment) is the system's state per the composition; its use (the strategy per the tipping, the platform per the cascade) is per the state's honesty.
- The hysteresis (path, memory, irreversibility) is the system's history per the state; its use (the strategy per the path, the intervention per the memory) is per the history's honesty.

## Phase 7 — Exercises and Assessment

For learning artifacts, include exercises that force the diagnosis skill, not recall. Use the standard set:

1. Spec the parameter for a viral system: the rate per the interaction, the threshold per the agent, the density per the network — with the measure per element.
2. Spec the order parameter: the fraction per the population, the alignment per the platform — with the measure per element.
3. Spec the transition: the continuous per the gradual, the discontinuous per the jump — with the example per phenomenon.
4. Run the early warning on a system: the slowing per the approach, the correlation per the proximity, the variance per the transition — with the state per the warning.
5. Run the hysteresis on a system: the path per the parameter, the memory per the transition, the irreversibility per the shift — with the record per the case.
6. Write the tipping's operating document: the parameter, order, transition, warning, and hysteresis specs — as the standing instrument, with the owner.

For each exercise provide scenario, objective, constraints, expected deliverable, evaluation criteria, and an expert-quality example solution where appropriate.

## Phase 8 — Quality Gate and Delivery

Before delivering, run the delivery checklists. The artifact fails the gate on any “no.”

### Parameter
- The rate is per the interaction
- The threshold is per the agent
- The density is per the network

### Order
- The fraction is per the population
- The alignment is per the platform
- The measure is per the element

### Transition
- The continuous is per the gradual
- The discontinuous is per the jump
- The critical is per the parameter

### Delivery gate
- The warning is per the element
- The hysteresis is per the history
- The record is per the case

## Anti-Patterns

the tipping as the promise; the threshold as the single number; the parameter without the estimate; the warning without the measure; the hysteresis unexamined; the limit unstated.

## Decision Heuristic

Before any claim or recommendation, ask:

1. What is the parameter per element, and what is the measure?
2. What is the order parameter per the state, and what is the fraction per the time?
3. What is the transition per the parameter, and what is the critical per element?
4. What is the warning per element, and what is the state per the warning?
5. What is the hysteresis per the history, and what is the record per the case?

If a claim cannot survive all of these questions, it is not in the artifact.

## Final Principle

The goal is not the tipping's prediction. It is the transition read per element — parameter per measure, order per state, shift per transition, warning per element, history per hysteresis — so the system's shift is read on its logic, and the logic says where it is sure.
